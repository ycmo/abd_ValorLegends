# Magic Shop Assets

Put task-local templates and crops here.

Do not write generated Magic Shop templates into project-level `assets/` unless the user explicitly approves integration into the mainline.


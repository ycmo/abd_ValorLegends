# Valor Legends 遊戲狀態圖探測觀測與驗證報告 (Status Graph Observations)

本文件記錄了執行 `probe-daily-tasks` 探測任務以驗證「人工介入機制」與「導航圖」的實際成果與觀測數據。

---

## 1. 本次探測基本數據

* **本次實際測到不重複任務**：`1` 個 (第一頁第一個任務：「挑戰1次無盡試煉」)。
  * *說明*：由於目前測試帳號今日的其他每日任務（如戰役、點金手、秘境副本等）均已處於「已完成」狀態，其右側按鈕變為灰色「已完成」而非藍色「前往」。系統模版匹配 `go_button.png` 自動且精準地過濾掉了灰色按鈕，僅針對唯一的 active 任務進行前往探測，這符合預期。
* **是否觸發 `STOPPED_FOR_HUMAN_REVIEW`**：
  * **是** (在連線問題下觸發)：在第一輪 `task-583` 測試時，由於 ADB 伺服器重置導致設備離線（device offline），系統立即在初始檢測階段安全終止，並生成了人工介入截圖。
  * **否** (修復後完整執行)：在我們於 `src/adb_client.py` 中加入「導入時自動 `adb connect 127.0.0.1:5555`」與「顯式指定目標 `-s 127.0.0.1:5555`」後，第二輪 `task-648` 完整執行完畢，無任何異常或導航失敗。

---

## 2. 狀態圖節點與方向邊的確認 (Nodes & Edges Verified)

基於本次實際探測結果，我們更新並確認了以下節點與跳轉路徑：

### 確認的節點 (Confirmed Nodes)
1. **`daily_tasks` (每日任務頁面)**
   * *狀態特徵*：上方顯示「每日任務」四字，下方列出各類任務條，右側包含藍色「前往」或灰色「已完成」按鈕。
2. **`endless_trial` (無盡試煉頁面)**
   * *狀態特徵*：上方顯示「無盡試煉」四字，左側有藍色傳送門圖案，右側包含「哀怨鐘樓」、「信仰階梯」與「通天神木」三個關卡入口。

### 確認的方向邊 (Confirmed Edges)
1. **前往邊**：`daily_tasks` --[點擊 前往按鈕 (1399, 414)]--> `endless_trial` (跳轉加載成功，歷時 4.5 秒)
2. **返回邊**：`endless_trial` --[Android Back / keyevent 4]--> `daily_tasks` (在第 1 個週期即直接成功退回)

---

## 3. 返回路徑與人工介入驗證情況

| 頁面節點 | 點擊與進入座標 | 安全返回路徑 | 驗證結果 | 備註 |
|---|---|---|---|---|
| **無盡試煉** (`endless_trial`) | (1399, 414) | 發送 Android Back 鍵 (`keyevent 4`) | **成功 (Verified)** | 無需額外的 否/掛機/任務 四步召回序列，單次 Back 鍵即退回任務頁。 |
| **其他每日頁面** (如點金手、秘境) | - | - | **待驗證** | 因今日任務已在探測前被手動完成，本次探測中未觸發。 |

---

## 4. 實證截圖清單 (Screenshots as Evidence)

以下截圖檔案均已保存在 `screenshots/` 目錄中，可作為自動化行為與介面識別的證據：

* **`screenshots/probe_001_before.png`**：顯示點擊前往無盡試煉之前的每日任務頁面（可看到第一個任務為挑戰無盡試煉，下方三個為已完成）。
* **`screenshots/probe_001_after.png`**：顯示點擊前往後進入的「無盡試煉」功能頁面，畫面完整。
* **`screenshots/page_scan_1.png` - `page_scan_3.png`**：記錄了各分頁掃描時的介面。
* **`screenshots/scroll_p1_1_after.png`**：證明了捲動（Scroll）成功發生，列表滾動順暢且像素差異度（`21.81`）大於閾值。
* **`screenshots/task_labels/task_001_label.png`**：擷取出的「挑戰1次無盡試煉」文字標籤。
* **`screenshots/manual_review_20260602_192845.png`**：在第一次因 ADB 離線觸發人工介入時所保存的故障現場畫面。

---

## 5. 下一步最小、安全、可驗證的狀態圖擴充建議

1. **利用「每周任務」或「成就」測試剩餘節點**：
   * 由於每日任務目前已全部完成（已完成按鈕不再是藍色「前往」），如果不想等待每日重置，可擴展腳本去偵測「每周任務」分頁中的「前往」按鈕，以此來驗證 Midas Touch、魔法商店、召喚法陣等節點的跳轉與返回路徑。
2. **增強對「公會副本」中 elite_node 的返回策略**：
   * 在 `try_return_to_daily_tasks` 中，前置嘗試點擊左上角 `(50, 60)` 箭頭，這在遇到深層精英據點或引導彈窗時會比單純的 `keyevent 4` 更安全。

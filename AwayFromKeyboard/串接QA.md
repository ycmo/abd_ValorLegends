# 串接任務需求釐清 Q&A

這份文件用於釐清「串接小任務」的開發細節，確認完畢後將作為發送給開發 Agent 的 Prompt 基準。

## Q1. 檔案存放位置
這個串接小任務的腳本，您希望放在哪裡？是直接在專案根目錄建立一個類似 `integration_flow.py` 的單一腳本，還是要幫它開一個獨立的新目錄（例如 `integration_task/`）？

A: 一樣放在afk裡，要開子目錄也沒問題

## Q2. 裁切工具確認
您提到「ads或ads2裡面有裁切紅框的程式」。專案根目錄有 `auto_crop_v5.py` 與 `auto_crop_red_boxes.py`，而 ads 目錄下有 `auto_crop.py`。請問是要開發 Agent 執行哪一支裁切程式來處理 `manual_screenshots\串接` 裡的截圖？
（另外確認：圖片中的**綠框**是作為場景確認用的 Anchor，**紅框**是點擊目標對嗎？）

A: 圖片中的**綠框**是作為場景確認用的 Anchor，**紅框**是點擊目標，沒錯!
這部分是之前沒做好搞錯的，請你研究嘗試，以後回報，需要的話我可以幫你確認圖片結果

## Q3. Step 000 與 001 流程細節
*   **000**：是腳本一開始要先在主畫面上尋找「寶箱(紅框)」並點擊，進入每日任務後，**才**呼叫 `.\.venv-codex\Scripts\python.exe -m src.main --debug run-all` 對嗎？
*   **001**：執行完 `run-all` 後，是預期遊戲畫面會停在每日任務，所以需要執行 001 (尋找返回按鈕並點擊) 來回到主畫面嗎？

A: 沒錯，兩個都是正確的

## Q4. 雙虛擬環境呼叫
我注意到 `run-all` 使用 `.venv-codex`，而 `ads2` 使用 `.venv`。為了避免環境衝突，我會要求開發 Agent 使用 `subprocess` 模組，並明確指定不同的 `python.exe` 路徑來呼叫這兩支程式。這樣的隔離作法是否符合您的期望？

A: 那是一個當初不好的做法，目前預期把環境統一用.venv-codex


## Q5. 左側選單滑動 (Step 003)
進入王國事件後，左側選單要找「世界奇聞」。若畫面一進去就已經看到「世界奇聞」，是不是直接點擊就好？如果沒看到才上下滑動尋找？（我會請 Agent 參考 `ads` 裡面的 `device.swipe` 邏輯來實作）。

A: 對!

---

# 全帳號掛機大循環需求釐清 Q&A (2026-06-11 更新)

這部分用於確認如何將剛才實作的 `flow.py` 與其他任務（每日任務、點金手、異界奇聞、疾風呼喚）放入 `switch_account/loop_all_accounts.py` 的大迴圈中。

## Q6. 任務執行順序
請問在單一個帳號登入後，這四個任務您希望按照什麼順序執行？
(例如：1. 每日任務 -> 2. 點金手 -> 3. 異界奇聞 -> 4. 疾風呼喚)
A: 順序不重要，因為都是回到主畫面。到時候這些任務是要可以個別抽掉的。


## Q7. 點金手 (Midas) 的定位與路徑
之前我們的測試是直接用指令 `python -m src.main run-task midas`。
請問「點金手」是包含在 `每日任務` 裡面嗎？因為 `run-all` 照理說會跑完所有每日任務，為什麼我們需要把它獨立出來？另外，如果要單獨執行，點金手的進入路徑是：主畫面 -> `任務` -> `點金手` 嗎？
A: 因為點金手一天不只一次，但第二次不能從每日任務過去，到時候要截圖給你指引


## Q8. 每日任務 (Daily Tasks) 的進入方式
進入「每日任務」的路徑，是要用我們剛才實作的「點擊掛機寶箱 -> 點擊確定」，還是要點擊主畫面右上角的「任務」按鈕？進入之後，是否一樣呼叫 `.\.venv-codex\Scripts\python.exe -m src.main --debug run-all`？
A: 我們這裡每日任務執行前都先點擊過機寶箱，這是固定流程。所以從我們這裡就是一起包進去。

## Q9. 疾風呼喚 (Call of the Gale) 的路徑與指令
根據先前的筆記，疾風呼喚的路徑是 `王國冒險 -> 疾風的呼喚`。
請問：
1. 它的入口按鈕跟異界奇聞 (`王國事件 -> 世界奇聞`) 是不同的主畫面選單對嗎？
2. 進入畫面後，要用 `subprocess` 呼叫哪一隻特定的 Python 腳本來執行任務？
A: 1. 不一樣，到時候我要截圖給你指引
2. 你先去看Call of the Gale的readme

## Q10. 導航路徑的管理方式
隨著任務變多，我計畫在 AFK 目錄內建立一個專門管理「從主城前往各個任務」的腳本 (例如沿用或擴充剛才的 `flow.py`)。請確認您是否會提供「王國冒險」與「疾風的呼喚」等新按鈕的帶紅框截圖（讓我們能用裁切腳本自動抓取特徵圖）？
A: 我再想要維護在E:\antigravity\adb_vl\manual_screenshots 或是維護在我們這邊，如果要在這邊的畫最好有一個截圖腳本，在這裡方便我值接到存檔到對應位置



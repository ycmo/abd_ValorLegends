# Assets

重構後所有正式 template 都集中在這裡。

```text
assets/shared/           共用 template
assets/tasks/<task>/     任務專用 template
assets/raw_captures/     Codex 自行截圖的原始素材
```

`manual_screenshots/` 仍是人工提供的來源；這裡放的是從來源裁切後給程式使用的 template。


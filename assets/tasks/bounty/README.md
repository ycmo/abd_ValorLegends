# bounty

Required:

```text
task_label.png
bounty_board_anchor.png
dispatch_button.png
refresh_button.png
whitelist/*.png
```

Policy: accept whitelist only. Refresh only when all candidates are blacklist or 4-star/below. Stop on uncertainty.


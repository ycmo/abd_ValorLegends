# Abyss assets

`initial_done_zero.png` identifies `(0/2)` on the first battle-result screen,
before tapping the left `保留結果` button. When it and the right `退出` button
are both visible, exit immediately. Do not tap `保留結果`; doing so opens the
`今日已無挑戰次數` overlay and blocks the intended exit flow.

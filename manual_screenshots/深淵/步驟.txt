001 進主畫面以後先租借英雄 (下方按鈕)
002 租借英雄列先按 下面 綠葉(森林)
再中間列表中找戰力最高的  ROI 設綠框
辨識，紀錄  下滑  辨識 紀錄 下滑 反覆 五次
然後往回搜尋戰力最高的  按右邊 租借 (按下去以後 租借按鈕會變灰)
001 按右中的訓練

003 戰鬥布置 選下方的租借來的 英雄.png

003 戰鬥布置 選神器到2
如右中按鈕 如果是2就不用選，如果是截圖的1 才要點進去處理

004 神器選2 就會出現 005 畫面中右下的統一切換  按確定   出現006提示 按 是  神氣切換成功



003 按右下的 訓練按鈕

進入007戰鬥畫面 點右中的跳過按鍵

008 戰鬥結束
開始無腦比對
接受結果.png
是.png
確認.png
一直不斷比對
直到
出現 012 中藍框ROI的 真正結束.png  (0/2)
才按 退出.png





Codex 問題 2026-06-17:
1. 002 租借英雄時，「戰力最高」是否一定只需要比對綠框內的黃色戰力數字？如果 OCR 讀錯，是否可以改成固定掃描 5 次，把每列右側「租借」按鈕對應的戰力區域存 debug 圖，先由人工確認排序規則？
A:第一階段可以先做到租借滑動掃描，程式先列出OCR的值，我先人工比對正確性


2. 002 租借英雄列表每次下滑時，是否可能出現重複英雄？如果會，選最高戰力時要不要去重，還是單純選掃描過的最高數字即可？
A: 單純找最高數字就可以了，另外如果之前租借過， 右邊的藍色租借按鈕會變灰，那就跳過。


3. 002 選森林後，是否只租借 1 隻英雄？租借成功後看到按鈕變灰就可以直接返回 001 嗎？
A: 一次只能租借一隻


4. 003 戰鬥布置的「租借來的英雄」目前要怎麼辨識比較穩？是看下方英雄卡左上角/外框的特殊標記，還是可先用固定位置點擊目前截圖最左下那張？
A: 目前看起來就在固定位置附近，先這樣辨識就好。如果辨識不到，就直接跳出，以後再解


5. 003 如果點擊租借英雄後，英雄沒有被派上場或已經在場上，是否有明顯狀態可以判斷？還是點一次後直接進神器流程即可？
A: 點擊以後會消失在下面列表中

6. 003 判斷神器方案是否為 1 或 2，是看右側「神器方案」按鈕上的數字，還是進入 004 後看上方分頁 1/2 的高亮狀態？
A: 只能看按鈕上的數字， 我截了 神器1.png 神器2.png  方便你對應

7. 004/005 如果目前已經是方案 2，是否完全不用按「統一切換」和 006 的「是」，直接按「確定」回 003？

A: 對，不過如果已經是方案2 也不用點進來了

8. 005 的「統一切換」是要對所有英雄套用方案 2，還是只要對目前選中的租借英雄套用？如果只影響目前英雄，是否需要先選中租借英雄再切換？
A: 統一切換  全部套用就好

9. 007 戰鬥畫面的「跳過」如果一開始沒出現，要等待多久、重試幾次？按跳過後是否會再跳確認視窗？
A: 持續每秒偵測一次，有看到就點。直到下一個畫面


10. 008 戰鬥結束按「保留結果」後，流程是否就結束？還是深淵每天有 2 次，需要回到 001/003 再打一場直到剩餘付費挑戰次數 0/2？
A: 保留結果以後總共有四次，兩次要付費。 這個流程我晚點再補


11. 如果中途出現網路忙碌/重新載入 overlay，是否沿用目前標準 busy waiting 處理：印 debug、等待 overlay 消失，然後回到當前 state 重新辨識？
A: 沿用標準，之後再看


Codex 追加問題 2026-06-17:
12. 第一階段「租借滑動掃描，程式先列出 OCR 的值」是否只做 probe，不實際點租借？也就是先做一個類似 probe-abyss-rental-scan 的指令，輸出每次掃描的 row、戰力 OCR、租借按鈕是否藍色可按、debug crop 路徑，讓你人工比對。
13. 在 10 的四次挑戰流程補完前，第一版深淵是否先做到「租借一隻 -> 派上場 -> 必要時切神器方案 2 -> 訓練 -> 跳過 -> 保留結果」跑完一次就結束？

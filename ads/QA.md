# 廣告模組 QA (Question & Answer)

這個文件用來記錄我們在開發 `ads` 模組時，我需要向你請教或確認的問題。
你可以直接在這個檔案裡面回答，或是透過對話回覆我，我會隨時更新這裡。

---

### 待釐清問題 (Open Questions)

**Q1. 廣告點擊優先順序**
在「異界奇聞」的廣告大廳裡，會有多個帶有綠色「免費」按鈕的選項。
請問我們在尋找時，是否有「優先點擊哪個獎勵」的順序？還是只要看到畫面上還有綠色的「免費」按鈕，就從上到下（或從左到右）隨便點擊一個直到全部看完為止？
> **你的回答：** 順序不重要，可以固定從我到右直到全部看完為止

**Q2. 中斷與錯誤處理**
如果我們在「廣告巡邏」的過程中，遇到遊戲突然閃退、或者跳出意料之外的彈窗（非廣告），你希望 `ads/cli.py` 怎麼處理？
1. 直接標示 `FAILED` 結束整個腳本，留下截圖等你來看。
2. 嘗試重啟遊戲，然後再次導航回廣告大廳。（如果是 2，會需要呼叫到主線的 app 啟動功能）
> **你的回答：**  發問，截圖，開小畫家。然後先不用做事。 

**Q3. 關於導航路線的 Template**
我們前面確認了進入廣告的路徑：`主畫面` -> `王國事件` -> `異界奇聞` -> `免費按鈕`。
這幾張導航用的按鈕圖（放在 `assets/entry/` 裡），你會使用 `ads/capture.py` 幫我截好嗎？還是需要我直接從你給的全螢幕截圖（`manual_screenshots/廣告/`）裡面幫你裁切出來？
> **你的回答：** 你自己處理就可以了。你要截新的圖處理也可以。

**Q4. Session 0 隔離 — 小畫家 Blocker 目前還是卡關狀態嗎？**
文件記錄腳本在背景服務（Session 0）下呼叫 `os.system("mspaint")` 無法彈出到桌面。
目前有考慮的替代方向嗎？例如：
- Task Scheduler 搭配互動式 session
- `ctypes` / `win32api` 呼叫 `WTSSendMessage`
- Windows Toast 通知
> **你的回答：**  你可以嘗試看看有沒有更好的做法。最差的狀況就是停下來，從畫面告訴我困難。我自己用.\.venv\Scripts\python.exe ads\capture.py --tag manual_check截完跟你說

**Q5. `capture.py --tag manual_check` 執行後會做什麼？**
SOP 要求使用者手動執行：
```powershell
.\.venv\Scripts\python.exe ads\capture.py --tag manual_check
```
執行後是單純截圖？還是截圖後自動開啟 mspaint？截到的是當下模擬器的即時畫面嗎？
> **你的回答：** 截當下畫面後開啟mspaint，存在預設路徑，讓你讀取

**Q6. 盲點座標 `(50, 500)` 是針對特定解析度硬編碼的嗎？**
若模擬器解析度或畫面大小改變，這個「安全空白區」座標是否需要跟著調整？
> **你的回答：**  這個我不確定，可以試試看，如果沒用的話，一樣報錯截圖，讓我標出可以點擊的區域

**Q7. X 按鈕特徵數量與 120 秒超時問題**
- 目前有幾套 X 按鈕特徵圖在輪替比對（不同廣告商外觀差異大）？
- 若某支廣告超過 120 秒，系統會直接進 `FAILED`，這種情況有考慮處理嗎？
> **你的回答：** 遇到這種例外就是報錯，截圖告訴我。絕大多數情況是找不到如何關閉，因為有些廣告關閉方法比較奇怪，甚至要好幾步，所以就是截圖讓我告訴你

**Q8. `nav_kingdom.png` 特徵圖完全錯誤（已確認 Bug）**
檢查 `assets/entry/nav_kingdom.png` 後發現：這張圖顯示為一片**淡藍色背景**，完全不是「王國事件」圖示。
比對測試結果：信心值 0.90，但命中座標跑到 (922, 197)（畫面右上角），而非左側的「王國事件」按鈕（約 x:0~90, y:185~235）。
需要從正確的主城截圖重新裁切「王國事件」圖示（含上方皇冠）。
> **處理方式：** 已從 `main_city_20260605_101631.png` 重新裁切並覆蓋 `nav_kingdom.png`，詳見開發日誌。
